import type { Variants } from "framer-motion";

export const providerVariants: Variants = {
  animate: {
    transition: {
      staggerChildren: 0.05,
    },
  },
  exit: {
    transition: {
      staggerChildren: 0.05,
    },
  },
};
