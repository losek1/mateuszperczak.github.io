import { AnimationProvider } from "@components/index";
import styled from "@emotion/styled";

export const StyledHomeAnimationProvider = styled(AnimationProvider)`
  display: grid;
  flex: 1;
  place-items: center;
`;
